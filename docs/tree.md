# Drzewo

```
Uruchom.ps1                 # to uruchamia użytkownik
URUCHOM.bat                 # dwuklik; woła Uruchom.ps1
README.md                   # instrukcja na start
LICENSE                     # MIT
PRIVACY.md SECURITY.md
config.example.json         # szablon, bez hasła i bez prawdziwej skrzynki
src/MailToContacts.ps1      # komendy (setup, run, demo, …)
src/modules/                # logika
src/scripts/Install-MailKit.ps1
schemas/                    # kontrakt JSON
samples/fetched/            # fikcyjne maile
samples/generated/          # wynik demo
samples/mailboxes/          # przykładowe listy folderów
tests/SelfTest.ps1
docs/                       # instrukcja i opis działania
lib/mailkit/                # DLL-e po instalacji (nie w gicie)
data/                       # wyniki żywych uruchomień (nie w gicie)
```

